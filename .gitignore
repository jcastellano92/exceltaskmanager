.claude/

# Confidential workbook data / internal planning — do NOT publish to this PUBLIC repo
*.xlsx
DASHBOARD-CAPACITY-NOTES.md
DASHBOARD-SPEC.md

# Local visual-test harness (mock Office/Sortable) — never publish
_harness.html
_mock.js
_*.png

# Host-filled manifests for local sideloading (keep the public repo's host generic)
manifest-*.local.xml
